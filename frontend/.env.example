# Google Maps JavaScript API key (used by the map + geocoding + places autocomplete).
# Copy this file to `.env` and fill in the value. Vite only exposes vars prefixed with VITE_.
VITE_GOOGLE_MAPS_API_KEY=
